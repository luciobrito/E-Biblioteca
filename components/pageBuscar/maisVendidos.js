import * as React from 'react';
import{Text, View, StyleSheet,TouchableOpacity,FlatList,Image,ScrollView,Pressable} from 'react-native';


export function MaisVend(){
  return(

    <ScrollView>
    <Text >Os Mais Vendidos</Text>
    </ScrollView>
  )
}